# [Decision title — short noun phrase naming what was decided]

> Copy this file to `docs/decisions/YYYY-MM-DD-<slug>.md` to start a new
> decision record. Reuse an existing plan/review's slug when this
> decision pairs with one, so the three artifacts correlate by filename
> alone. `TEMPLATE.md` itself is excluded from `PROJECT_INDEX.md`'s index
> and from file-count verification checks.
>
> Keep this lean — closer to a 4-6 section ADR than a heavyweight one.
> When a paired plan/review already exists, this file is a short
> synthesis that links out to it, not a third retelling.

**Date:** YYYY-MM-DD

## Context

[What prompted this decision — the problem, the constraint, or the
failing case.]

## Decision

[What was actually done — the concrete outcome, in a sentence or two.]

## Why

[The non-obvious reasoning behind the decision. Name alternatives
considered only when a real fork actually existed — don't force an
empty "alternatives" subsection when there was only one reasonable
path.]

## Files touched

[The files the change actually modified — a short list, not a diff.]

## Verification

[How this was confirmed correct: tests run, live spot-checks performed,
eval re-runs — or a pointer to the paired plan/review's own Verification
section if it already covers this in full.]

## Related

[Links to the plan/review that led here, or a prior decision this
amends (e.g. "Amends `docs/decisions/2026-09-01-<slug>.md`").]
